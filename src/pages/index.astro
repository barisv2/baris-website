---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog'))
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
  .slice(0, 5);
---

<html lang="en">
  <head>
    <BaseHead title="Barış Gürbüzler" description="Co-founder & CEO of Hardal. Writing about products, privacy, and the web." />
  </head>
  <body>
    <Header />
    <main>
      <p class="eyebrow">Barcelona · Co-founder & CEO, Hardal</p>
      <h1 class="hero-title">
        I don't know a lot<br />
        of things. Here's what<br />
        <em>I'm finding out.</em>
      </h1>
      <p class="hero-bio">
        Building <a href="https://usehardal.com" target="_blank">Hardal</a> — privacy-first analytics
        for teams that give a damn. Writing about products, privacy, and what it means to build with care.
      </p>

      <p class="section-label">Recent writing</p>
      <ul class="post-list">
        {posts.map(post => (
          <li>
            <div class="post-left">
              <a href={`/blog/${post.id.replace('.md', '')}/`}>{post.data.title}</a>
              <span class={`tag ${post.data.tags?.includes('note') ? 'tag-note' : 'tag-essay'}`}>
                {post.data.tags?.includes('note') ? 'Note' : 'Essay'}
              </span>
            </div>
            <span class="post-date">
              {post.data.pubDate.toLocaleDateString('en-US', { month: 'short', year: 'numeric' })}
            </span>
          </li>
        ))}
      </ul>
    </main>
    <Footer />
  </body>
</html>
